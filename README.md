# MARSHAL — Frontend

React app, built with Vite — not Create React App, which the React
team deprecated for new projects in Feb 2025.

## Right now (Phase 1)
Just .gitignore and .env.example — repo and git workflow exist
before any app code does.

## Coming in Phase 4
    npm create vite@latest . -- --template react
    npm install
    cp .env.example .env
    npm run dev
